<!DOCTYPE html>

<html>
    <head>
        <title>404</title>
        <link rel='icon' href='/img/weather.png'>
    </head>
    <body>
        {{>header}}
        <h4>{{message}}</h4>
        {{>footer}}
    </body>
</html>